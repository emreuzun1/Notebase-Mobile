import React from 'react';
import { Background } from '../../components/Background/Background';

export const Settings = () => {
    return (
        <Background>
            
        </Background>
    )
}